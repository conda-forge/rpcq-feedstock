{% set name = "rpcq" %}
{% set version = "3.9.2" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/rpcq-{{ version }}.tar.gz
  sha256: 690c6e5ca8b2a6831c4f34e527b3adb276ce8656f43e851dfd90bc58961f5a69

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - msgpack-python >=0.6,<1.0
    - python >=3.6
    - python-rapidjson
    - pyzmq >=17
    - ruamel.yaml

test:
  imports:
    - rpcq
    - rpcq.external
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://www.rigetti.com
  license: Apache-2.0
  license_family: Apache
  license_file: LICENSE
  summary: The RPC framework and message specification for Rigetti QCS
  dev_url: https://github.com/rigetticomputing/rpcq

extra:
  recipe-maintainers:
    - jmackeyrigetti
    - kilimanjaro
    - notmgsk
    - BastianZim
